<%- if custom_css_styles.present? %>
<%= raw custom_css_styles %>

<%- end %>
<%- @user_roles.each do |role| %>
.user-role-<%= role.id %> {
  --user-role-accent: <%= role.color %>;
  --user-role-background: <%= role.color + '39' %>;
  --user-role-border: <%= role.color %>;
}

<%- end %>
